using namespace std;
#include <iostream>

int main()
{
    int x, y;
    cin >> x >> y;
    long long a, b;
    a = x;
    b = y;

    cout << x << " + " << y << " = " << x + y << endl;
    cout << a << " * " << b << " = " << a * b << endl;
    cout << x << " - " << y << " = " << x - y;
}